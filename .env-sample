# PowerBody SOAP (Dropshipping API v1.5)
POWERBODY_API_WSDL="http://www.powerbody.co.uk/api/soap/?wsdl"
POWERBODY_USER=""
POWERBODY_PASS=""

# Shopify Admin REST/GraphQL API
SHOPIFY_STORE=""
SHOPIFY_ACCESS_TOKEN=""
# Find your location_id in Shopify Admin: Settings > Locations, or via API
# This must be a valid numeric ID, not 0
SHOPIFY_LOCATION_ID=""

# Product Settings
PRICE_MARKUP_PERCENT="22"        # Markup percentage to apply to PowerBody prices (e.g., 22 = 22% markup)
DEFAULT_VENDOR="Powerbody"
DEFAULT_PRODUCT_TYPE="Supplement"

# Sync Settings
LOG_LEVEL="INFO"                  # DEBUG / INFO / WARNING / ERROR
SYNC_INTERVAL="3600"             # Sync interval in seconds (default: 1 hour)

# Cron Schedule (if using cron jobs)
PRODUCT_SYNC_CRON="0 2 * * *"     # daily at 02:00, products
ORDER_SYNC_CRON="0 * * * *"       # hourly, orders + returns + comments

# Sync Settings



PB_API_CLIENT=694cc0c660b3ba0e1b1e71517e1ca8a3
PB_API_SECRET=b4fe69a51d814fa3457954837a081255
# Sync Settings

# Product Mapping Settings

SYNC_INTERVAL=3600 # In seconds (default: 1 hour)
LOG_LEVEL=INFO # DEBUG, INFO, WARNING, ERROR

# Product Mapping Settings

DEFAULT_PRODUCT_TYPE=Supplement
DEFAULT_VENDOR=Powerbody

# Order Settings
ORDER_REQUIRED_TAGS=""           # Comma-separated list of required tags for orders to be sent to PowerBody (leave empty to send only orders with NO tags) 